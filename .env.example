# =============================================================================
# AI Model Provider Configuration
# =============================================================================
# Choose which AI provider to use: 'anthropic' for Claude or 'z-ai' for GLM
API_PROVIDER=anthropic

# =============================================================================
# Anthropic Configuration (Claude Models)
# =============================================================================
# Required when API_PROVIDER=anthropic
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-your-key-here

# =============================================================================
# Z.AI Configuration (GLM Models)
# =============================================================================
# Required when API_PROVIDER=z-ai
# Get your API key from: https://z.ai

# Step 1: Set your Z.AI API key
ZAI_API_KEY=your-zai-key-here

# Step 2: When using Z.AI, uncomment these lines:
# ANTHROPIC_BASE_URL=https://api.z.ai/api/anthropic
# ANTHROPIC_AUTH_TOKEN=${ZAI_API_KEY}

# Note: Z.AI uses an Anthropic-compatible API endpoint, so we override
# the ANTHROPIC_* environment variables to point to Z.AI's proxy.

# =============================================================================
# Email Configuration (Optional - for email features)
# 
